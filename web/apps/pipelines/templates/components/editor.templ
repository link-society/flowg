package components

type EditorProps struct {
  Flow string
}

templ Editor(props EditorProps) {
  <div class="col s10 h-full flex flex-col">
    <div
      class="
        card-panel white
        p-0 mb-0 h-0
        flex-grow flex-shrink
      "
    >
      <flow-editor id="data_pipeline_flow" flow={props.Flow}></flow-editor>
    </div>
  </div>
}
