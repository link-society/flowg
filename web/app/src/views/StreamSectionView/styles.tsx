import { styled } from '@mui/material'

import AppContainer from '@/components/AppContainer/component'

export const StreamSectionViewContainer = styled(AppContainer)({
  placeContent: 'center',
  '& h1': {
    fontWeight: 700,
    textAlign: 'center',
  },
})
