import { styled } from '@mui/material'

export const ForwarderEditorAzureMonitorRoot = styled('div')(({ theme }) => ({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'stretch',
  gap: theme.spacing(1.5),
}))
