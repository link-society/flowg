import { Button, styled } from '@mui/material'

export const NavMenuProfileContainer = styled('div')`
  display: contents;
`

export const NavMenuProfileButton = styled(Button)`
  text-transform: initial;
`
