import { Button, styled } from '@mui/material'

export const NavMenuSettingsContainer = styled('div')`
  display: contents;
`

export const NavMenuSettingsButton = styled(Button)`
  text-transform: initial;
`
