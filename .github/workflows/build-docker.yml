---
name: build-docker

on:
  workflow_call:
    inputs:
      ref:
        required: true
        type: string

jobs:
  build:
    strategy:
      matrix:
        include:
          - ghrunner: ubuntu-latest
            arch: "amd64"
            os: "linux"
          - ghrunner: ubuntu-24.04-arm
            arch: "arm64"
            os: "linux"

    runs-on: ${{ matrix.ghrunner }}

    steps:
      - name: checkout@scm
        uses: actions/checkout@v5

      - name: setup@docker
        uses: docker/setup-buildx-action@v3

      - name: login@docker
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: build@docker
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/flowg.dockerfile
          build-args: |
            UPX_ARCH=${{ matrix.arch }}
            UPX_OS=${{ matrix.os }}
          tags: ghcr.io/link-society/flowg/ci-artifacts/docker:${{ inputs.ref }}-${{ matrix.os }}-${{ matrix.arch }}
          push: true
          load: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
