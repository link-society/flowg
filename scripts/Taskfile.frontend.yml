version: '3'

tasks:
  build:
    internal: true
    dir: ./web/app
    cmds:
      - npm i
      - npm run build:dev
      - mkdir -p ../public
      - cp -r ./dist/* ../public/
    env:
      NODE_ENV: dev
